custom:
  stage: ${self:provider.stage}
  projectName: iam
#   memSize:
#     dev: 512
#     uat: 512
#     prod: 1024
  artifactBucket: xplor-${self:custom.common.stage}-${self:custom.common.projectName}-layers-bucket
  secretsManager:
    dev: dev/xplor-iam
